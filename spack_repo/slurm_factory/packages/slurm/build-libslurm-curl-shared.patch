--- a/src/curl/Makefile.am
+++ b/src/curl/Makefile.am
@@ -4,10 +4,13 @@ AUTOMAKE_OPTIONS = foreign
 
 AM_CPPFLAGS = -I$(top_srcdir)
 
 if WITH_CURL
 
-noinst_LTLIBRARIES = libslurm_curl.la
+# Build as installed shared library so plugins can link against it at runtime
+# Originally was noinst_LTLIBRARIES which only built static .a file
+lib_LTLIBRARIES = libslurm_curl.la
 libslurm_curl_la_SOURCES = slurm_curl.c slurm_curl.h
 libslurm_curl_la_LIBADD = $(LIB_CURL)
+libslurm_curl_la_LDFLAGS = -version-info 0:0:0
 
 force:
 $(libslurm_curl_la_LIBADD) : force
